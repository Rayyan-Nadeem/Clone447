import type { MantineTheme } from '@mantine/core/lib/core/MantineProvider';
export declare function getSortedBreakpoints(breakpoints: string[], theme: MantineTheme): {
    value: string;
    px: number;
}[];
