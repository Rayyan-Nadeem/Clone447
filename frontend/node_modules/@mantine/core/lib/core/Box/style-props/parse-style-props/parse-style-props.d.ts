import { MantineTheme } from '@mantine/core/lib/core/MantineProvider';
import type { SystemPropData } from '@mantine/core/lib/core/Box/style-props/style-props-data';
import type { StyleProp } from '@mantine/core/lib/core/Box/style-props/style-props.types';
import type { SortMediaQueriesResult } from '@mantine/core/lib/core/Box/style-props/parse-style-props/sort-media-queries';
interface ParseStylePropsOptions {
    styleProps: Record<string, StyleProp<any>>;
    theme: MantineTheme;
    data: Record<string, SystemPropData>;
}
export interface ParseStylePropsResult {
    hasResponsiveStyles: boolean;
    inlineStyles: React.CSSProperties;
    styles: React.CSSProperties;
    media: Record<string, React.CSSProperties>;
}
export declare function parseStyleProps({ styleProps, data, theme, }: ParseStylePropsOptions): SortMediaQueriesResult;
export {};
