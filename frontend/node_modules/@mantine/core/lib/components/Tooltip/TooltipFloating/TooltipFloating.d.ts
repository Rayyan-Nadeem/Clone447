import { Factory } from '@mantine/core/lib/core';
import { TooltipBaseProps, TooltipCssVariables, TooltipStylesNames } from '@mantine/core/lib/components/Tooltip/Tooltip.types';
export interface TooltipFloatingProps extends TooltipBaseProps {
    /** Offset from mouse in px, `10` by default */
    offset?: number;
    /** Uncontrolled tooltip initial opened state */
    defaultOpened?: boolean;
}
export type TooltipFloatingFactory = Factory<{
    props: TooltipFloatingProps;
    ref: HTMLDivElement;
    stylesNames: TooltipStylesNames;
    vars: TooltipCssVariables;
}>;
export declare const TooltipFloating: import("@mantine/core/lib/core").MantineComponent<{
    props: TooltipFloatingProps;
    ref: HTMLDivElement;
    stylesNames: TooltipStylesNames;
    vars: TooltipCssVariables;
}>;
