import { Factory } from '@mantine/core/lib/core';
import { ModalBaseCloseButtonProps, ModalBaseOverlayProps } from '@mantine/core/lib/components/ModalBase';
import { ModalBody } from '@mantine/core/lib/components/Modal/ModalBody';
import { ModalCloseButton } from '@mantine/core/lib/components/Modal/ModalCloseButton';
import { ModalContent } from '@mantine/core/lib/components/Modal/ModalContent';
import { ModalHeader } from '@mantine/core/lib/components/Modal/ModalHeader';
import { ModalOverlay } from '@mantine/core/lib/components/Modal/ModalOverlay';
import { ModalRoot, ModalRootCssVariables, ModalRootProps, ModalRootStylesNames } from '@mantine/core/lib/components/Modal/ModalRoot';
import { ModalTitle } from '@mantine/core/lib/components/Modal/ModalTitle';
export type ModalStylesNames = ModalRootStylesNames;
export type ModalCssVariables = ModalRootCssVariables;
export interface ModalProps extends ModalRootProps {
    __staticSelector?: string;
    /** Modal title */
    title?: React.ReactNode;
    /** Determines whether the overlay should be rendered, `true` by default */
    withOverlay?: boolean;
    /** Props passed down to the `Overlay` component, use to configure opacity, `background-color`, styles and other properties */
    overlayProps?: ModalBaseOverlayProps;
    /** Modal content */
    children?: React.ReactNode;
    /** Determines whether the close button should be rendered, `true` by default */
    withCloseButton?: boolean;
    /** Props passed down to the close button */
    closeButtonProps?: ModalBaseCloseButtonProps;
}
export type ModalFactory = Factory<{
    props: ModalProps;
    ref: HTMLDivElement;
    stylesNames: ModalStylesNames;
    vars: ModalCssVariables;
    staticComponents: {
        Root: typeof ModalRoot;
        Overlay: typeof ModalOverlay;
        Content: typeof ModalContent;
        Body: typeof ModalBody;
        Header: typeof ModalHeader;
        Title: typeof ModalTitle;
        CloseButton: typeof ModalCloseButton;
    };
}>;
export declare const Modal: import("@mantine/core/lib/core").MantineComponent<{
    props: ModalProps;
    ref: HTMLDivElement;
    stylesNames: ModalStylesNames;
    vars: ModalCssVariables;
    staticComponents: {
        Root: typeof ModalRoot;
        Overlay: typeof ModalOverlay;
        Content: typeof ModalContent;
        Body: typeof ModalBody;
        Header: typeof ModalHeader;
        Title: typeof ModalTitle;
        CloseButton: typeof ModalCloseButton;
    };
}>;
